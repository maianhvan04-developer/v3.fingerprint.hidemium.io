API_URL=http://localhost:5000
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Fingerprint Pro browser-agent key (safe to expose to the browser).
NEXT_PUBLIC_FINGERPRINT_API_KEY=
# Choose the region configured for your Fingerprint application: us, eu, or ap.
NEXT_PUBLIC_FINGERPRINT_REGION=ap
# Fingerprint Server API key. Keep this server-only and never prefix it with NEXT_PUBLIC_.
FINGERPRINT_SECRET_API_KEY=
